# DB 관련 파일 제외
/models/db.php

# 개인 개발 환경 설정 파일 제외 (VS Code)
.vscode/

# 민감한 서비스 파일 제외 (services 폴더 전체)
services/

# 기타 제외할 가능성이 있는 파일/폴더
*.sql    # 데이터베이스 백업 파일 제외
.env     # 환경 변수 파일 제외
vendor/  # PHP Composer 라이브러리 제외
node_modules/  # npm 라이브러리 제외
